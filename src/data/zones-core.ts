import { sql } from "~/db";
import { randomUUID } from "node:crypto";
export type ZoneActor={orgId:string;id:string;role:string};
const qdb=()=>sql();
function localDate(tz:string, d=new Date()){ return new Intl.DateTimeFormat('en-CA',{timeZone:tz,year:'numeric',month:'2-digit',day:'2-digit'}).format(d); }
export function zoneSelectionOpenAt(nowIso:string|Date, tz:string){ return Number(new Intl.DateTimeFormat('en-US',{timeZone:tz,hour:'numeric',hour12:false}).format(new Date(nowIso)))%24 >= 6; }
function can(a:ZoneActor){return a.role==='owner'||a.role==='admin';}
export async function selectZoneCore(a:ZoneActor, zoneId:string, now=new Date()){
 const q=qdb(); const z=await q`SELECT id,name,tz FROM dispatch_zones WHERE id=${zoneId} AND org_id=${a.orgId} AND active=TRUE LIMIT 1`;
 if(!z.length)return {ok:false as const,message:'That zone is not available.'};
 if(!zoneSelectionOpenAt(now, String(z[0].tz)))return {ok:false as const,message:'Zone selection opens at 6:00 AM local'};
 const day=localDate(String(z[0].tz),now); const rows=await q`SELECT zone_change_count FROM driver_availability_log WHERE org_id=${a.orgId} AND user_id=${a.id} AND day=${day} LIMIT 1`;
 const count=rows.length?Number(rows[0].zone_change_count):0; if(count>=2)return {ok:false as const,message:'You can change your zone only once per day.'};
 await q`INSERT INTO driver_availability_log(org_id,user_id,day,zone_id,zone_changed_at,zone_change_count) VALUES(${a.orgId},${a.id},${day},${zoneId},NOW(),1) ON CONFLICT ON CONSTRAINT driver_availability_log_pkey DO UPDATE SET zone_id=${zoneId},zone_changed_at=NOW(),zone_change_count=driver_availability_log.zone_change_count+1,updated_at=NOW()`;
 return {ok:true as const,zoneId};
}
async function stats(q:any, z:any, day=localDate(String(z.tz))){
 const drivers=await q`SELECT COUNT(*)::int n FROM driver_availability_log WHERE org_id=${z.org_id} AND day=${day} AND session_started_at IS NOT NULL AND zone_id=${String(z.id)}`;
 const jobs=await q`SELECT status,lat,lng,pickup_lat,pickup_lng FROM dispatch_jobs WHERE org_id=${z.org_id} AND created_at>=NOW()-INTERVAL '24 hours' AND (pickup_lat IS NOT NULL AND pickup_lng IS NOT NULL OR lat IS NOT NULL AND lng IS NOT NULL)`;
 const inside=(j:any)=>{const la=Number(j.pickup_lat??j.lat),ln=Number(j.pickup_lng??j.lng);const x=(la-Number(z.lat))*69,y=(ln-Number(z.lng))*69*Math.cos(Number(z.lat)*Math.PI/180);return Math.sqrt(x*x+y*y)<=Number(z.radius_miles)};
 const inJobs=jobs.filter(inside), active=inJobs.filter((j:any)=>['offered','assigned','in_progress'].includes(String(j.status))).length, unassigned=inJobs.filter((j:any)=>j.status==='new').length, av=Number(drivers[0]?.n??0), ratio=(active+unassigned)/Math.max(av,1), busyness=av===0&&(active+unassigned)>0?'Busy':ratio>=2?'Busy':ratio>=1?'Moderate':'Low';
 return {busyness,availableDrivers:av,activeJobs:active,unassignedJobs:unassigned,recentVolume24h:inJobs.length,demandRatio:Number(ratio.toFixed(1))};
}
export async function getZonesCore(a:ZoneActor){ const q=qdb(); const zones=await q`SELECT id,org_id,name,lat,lng,radius_miles,tz FROM dispatch_zones WHERE org_id=${a.orgId} AND active=TRUE ORDER BY sort_order,name`; const out=[]; for(const z of zones) out.push({id:String(z.id),name:String(z.name),radiusMiles:Number(z.radius_miles),tz:String(z.tz),...(await stats(q,z))}); return out; }
export async function getMyZoneStateCore(a:ZoneActor, now=new Date()){
 const q=qdb(); const zones=await q`SELECT id,name,tz FROM dispatch_zones WHERE org_id=${a.orgId} AND active=TRUE ORDER BY sort_order,name`;
 const logs=await q`SELECT zone_id,zone_changed_at,zone_change_count,day,updated_at FROM driver_availability_log WHERE org_id=${a.orgId} AND user_id=${a.id} ORDER BY updated_at DESC`;
 const selected=logs.find((r:any)=>{const zone=zones.find((x:any)=>String(x.id)===String(r.zone_id));return localDate(String(zone?.tz??'America/New_York'))===String(r.day)}) ?? logs[0];
 const zid=selected?.zone_id?String(selected.zone_id):null; const z=zones.find((x:any)=>String(x.id)===zid)??zones[0]; const count=selected?Number(selected.zone_change_count??0):0;
 return {ok:true as const,zoneId:zid,zoneName:zid?(String(z?.name??null)):null,zoneChangedAt:selected?.zone_changed_at?new Date(selected.zone_changed_at).toISOString():null,zoneChangeCount:count,canChangeToday:count<1,selectionOpen:z?zoneSelectionOpenAt(now,String(z.tz)):false};
}
export async function getDispatchZonesForOwnerCore(a:ZoneActor){if(!can(a))return {ok:false as const,message:'Owner access required.'};const q=qdb();const zones=await q`SELECT id,org_id,name,lat,lng,radius_miles,tz,active,sort_order FROM dispatch_zones WHERE org_id=${a.orgId} ORDER BY sort_order,name`;const out=[];for(const z of zones){const s=await stats(q,z);const day=localDate(String(z.tz));const c=await q`SELECT COUNT(DISTINCT user_id)::int n FROM driver_availability_log WHERE org_id=${a.orgId} AND day=${day} AND session_started_at IS NOT NULL AND zone_id=${String(z.id)}`;out.push({id:String(z.id),name:String(z.name),lat:Number(z.lat),lng:Number(z.lng),radiusMiles:Number(z.radius_miles),tz:String(z.tz),active:Boolean(z.active),sortOrder:Number(z.sort_order),assignedDriverCount:Number(c[0]?.n??0),...s});}return {ok:true as const,zones:out};}
export async function getOwnerZoneDriverRosterCore(a:ZoneActor){if(!can(a))return {ok:false as const,message:'Owner access required.'};const q=qdb();const rows=await q`SELECT u.id user_id,u.name,(u.deactivated_at IS NULL) active,l.session_started_at,l.zone_id,z.name zone_name,l.zone_changed_at FROM users u JOIN organization_memberships m ON m.user_id=u.id AND m.org_id=${a.orgId} LEFT JOIN LATERAL (SELECT * FROM driver_availability_log x WHERE x.org_id=${a.orgId} AND x.user_id=u.id ORDER BY x.day DESC,x.updated_at DESC LIMIT 1) l ON TRUE LEFT JOIN dispatch_zones z ON z.id=l.zone_id WHERE m.role='contractor' ORDER BY u.name`;return {ok:true as const,drivers:rows.map((r:any)=>({userId:String(r.user_id),name:String(r.name??''),active:Boolean(r.active),online:Boolean(r.session_started_at),zoneId:r.zone_id?String(r.zone_id):null,zoneName:r.zone_name?String(r.zone_name):null,zoneChangedAt:r.zone_changed_at?new Date(r.zone_changed_at).toISOString():null}))};}
export async function ownerSetZoneCore(a:ZoneActor,userId:string,zoneId:string|null,day?:string){ if(!can(a))return {ok:false as const,message:'Owner access required.'}; const d=day??new Date().toISOString().slice(0,10); if(zoneId){const q=qdb(); const z=await q`SELECT id FROM dispatch_zones WHERE id=${zoneId} AND org_id=${a.orgId} AND active=TRUE`;if(!z.length)return {ok:false as const,message:'Zone not found.'};} const q=qdb(); await q`INSERT INTO driver_availability_log(org_id,user_id,day,zone_id,zone_changed_at,zone_change_count) VALUES(${a.orgId},${userId},${d},${zoneId},NOW(),0) ON CONFLICT ON CONSTRAINT driver_availability_log_pkey DO UPDATE SET zone_id=${zoneId},zone_changed_at=NOW(),updated_at=NOW()`; await q`INSERT INTO audit_log(id,org_id,actor_user_id,actor_role,action,entity_type,entity_id,detail) VALUES(${randomUUID()},${a.orgId},${a.id},${a.role},'driver_zone_override','driver',${userId},${JSON.stringify({zoneId,day:d,reason:'owner/admin override'})}::jsonb)`; return {ok:true as const}; }
export async function upsertZoneCore(a:ZoneActor,v:any){if(!can(a))return {ok:false as const,message:'Owner access required.'};const q=qdb();const id=String(v.id??randomUUID());await q`INSERT INTO dispatch_zones(id,org_id,name,lat,lng,radius_miles,tz,active,sort_order) VALUES(${id},${a.orgId},${v.name},${v.lat},${v.lng},${v.radiusMiles??20},${v.tz??'America/New_York'},${v.active??true},${v.sortOrder??0}) ON CONFLICT(id) DO UPDATE SET name=${v.name},lat=${v.lat},lng=${v.lng},radius_miles=${v.radiusMiles??20},tz=${v.tz??'America/New_York'},active=${v.active??true},sort_order=${v.sortOrder??0} WHERE dispatch_zones.org_id=${a.orgId}`;return {ok:true as const,id};}
