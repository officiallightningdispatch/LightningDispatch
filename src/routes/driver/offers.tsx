import { createFileRoute } from "@tanstack/react-router"; import { PlaceholderRoute } from "~/components/app-shell";
export const Route=createFileRoute("/driver/offers")({component:()=> <PlaceholderRoute title="driver/offers" description="Offers"/>});
