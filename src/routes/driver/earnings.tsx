import { createFileRoute } from "@tanstack/react-router"; import { PlaceholderRoute } from "~/components/app-shell";
export const Route=createFileRoute("/driver/earnings")({component:()=> <PlaceholderRoute title="driver/earnings" description="Earnings"/>});
