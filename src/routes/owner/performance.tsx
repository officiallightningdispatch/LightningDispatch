import { createFileRoute } from "@tanstack/react-router"; import { PlaceholderRoute } from "~/components/app-shell";
export const Route=createFileRoute("/owner/performance")({component:()=> <PlaceholderRoute title="owner/performance" description="Performance"/>});
