import { validateZoneStatus, clampDemandLevel, validateHexColor, validateUnlockJobs, demandLevelToColor, computedDemandLevel } from './src/lib/zone-model.ts';
let checks=0; const ok=(x,m)=>{if(!x)throw Error(m);checks++};
for(const [v,h,o] of [[0,'#FDB268',.22],[24,'#FDB268',.22],[25,'#F27801',.32],[49,'#F27801',.32],[50,'#DC2626',.42],[74,'#DC2626',.42],[75,'#991B1B',.54],[100,'#991B1B',.54]]) {const c=demandLevelToColor(v);ok(c.hex===h&&c.opacity===o,`band ${v}`)}
ok(demandLevelToColor(null).opacity===.14,'null');ok(clampDemandLevel(-4)===0&&clampDemandLevel(140)===100,'clamp');ok(validateZoneStatus('reserved')==='reserved','status');try{validateZoneStatus('x');throw Error('bad')}catch{};ok(validateHexColor('#f27801')==='#F27801','hex');try{validateHexColor('red');throw Error('bad')}catch{};ok(validateUnlockJobs(0)===0,'unlock');try{validateUnlockJobs(-1);throw Error('bad')}catch{};ok(computedDemandLevel(2,1,10,2)===30,'capacity');ok(computedDemandLevel(2,1,null,2)===150>100?'x':computedDemandLevel(2,1,null,2)===100,'drivers fallback');ok(computedDemandLevel(0,0,null,0)===null,'unavailable');console.log(`ALL PASSED / ${checks} checks`);
